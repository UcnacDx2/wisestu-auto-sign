# .goreleaser.yaml
# Visit https://goreleaser.com/customization/ for more options
project_name: zhxg-signin

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy

builds:
  - main: ./cmd/zhxg-signin/
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    binary: '{{ .ProjectName }}_v{{ .Version }}'

archives:
  - id: linux-macos
    format: tar.gz
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    files:
      - configs/config.yaml.example
      - README.md
    builds:
      - zhxg-signin
    goos:
      - linux
      - darwin
  - id: windows
    format: zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    files:
      - configs/config.yaml.example
      - README.md
    builds:
      - zhxg-signin
    goos:
      - windows

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'